
  # web apmul

  This is a code bundle for web apmul. The original project is available at https://www.figma.com/design/rw9qi9yDmC8ORmOVJN6mf0/web-apmul.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  